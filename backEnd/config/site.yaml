# ═══════════════════════════════════════════════════════════════════
#  站点配置 (site.yaml)
#  定义 Walter 系统中所有站点的信息
# ═══════════════════════════════════════════════════════════════════

# 默认租约时长（秒）
lease_duration: 60.0

# 站点列表
sites:
  - id: 0
    name: "server00"
    host: "127.0.0.1"
    port: 5000

  - id: 1
    name: "server01"
    host: "127.0.0.1"
    port: 5001
